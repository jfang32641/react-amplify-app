import React from 'react'
import PropTypes from 'prop-types'

function RFCP(props) {
    return (
        <div>RFCP</div>
    )
}

RFCP.propTypes = {
    hello: PropTypes.string
}

export default RFCP
